# Sections

This file defines section ordering, impact levels, and filename prefixes.

---

## 1. Schema Definition (definition)

**Impact:** HIGH
**Description:** How to define Zod schemas for domain models — base extension, naming, type inference, and enum integration.

## 2. Schema Usage (usage)

**Impact:** HIGH
**Description:** How to compose, derive, and instantiate schemas — nesting, pick/partial, and createModel.
